# This is a sample config
# Create a file called config and fill in the appropriate fields as follows:

API_KEY = ('YOUR_API_KEY_HERE',)
ENDPOINT = ('YOUR_ENDPOINT_HERE',)
PORT = ('YOUR_PORT_NUMBER_HERE',)

# NOTE: Remove API key from ENDPOINT query params
# NOTE: Remove quotes from PORT and leave as a number
# NOTE: Ensure the PORT is valid (a value ranging from 0 to 65535)
